# Admin Portal — copy to .env.local
# API base URL (use proxy in dev, full URL in prod)
VITE_ADMIN_API_URL=/admin/api
# Production (Vercel): VITE_ADMIN_API_URL=https://api.saradhi.katakam.in/admin/api
